{
  "scripts": {
    "build": "chromium-browser --headless --print-to-pdf index.html && qpdf output.pdf --split-pages=1 resume.pdf && rm resume-2.pdf output.pdf && mv resume-1.pdf resume.pdf"
  },
  "devDependencies": {
    "live-server": "^1.2.0"
  }
}
